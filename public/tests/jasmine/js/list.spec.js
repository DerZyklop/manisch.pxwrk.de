// Generated by CoffeeScript 1.6.3
(function() {
  describe('class List', function() {
    var list;
    list = new List;
    it('should be defined', function() {
      return expect(list).toBeDefined();
    });
    it('should be possible to fetch items with options', function() {
      list.fetch({
        success: function() {}
      });
      return expect(list.length).toBeGreaterThan(1);
    });
    beforeEach(function() {
      return list.fetch();
    });
    describe('fetch()', function() {
      return it('should get more then one item', function() {
        return expect(list.length).toBeGreaterThan(1);
      });
    });
    describe("category()", function() {
      it("should find as many 'alle' as length of list", function() {
        var newLength;
        newLength = list.category('alle').length;
        return expect(newLength).toEqual(list.length);
      });
      return it("should find less 'nomen' then 'alle'", function() {
        var newLength;
        newLength = list.category('nomen').length;
        return expect(newLength).toBeLessThan(list.length);
      });
    });
    return describe("search()", function() {
      return it("should find a 'Moss' in the List", function() {
        var newLength;
        newLength = list.search('Moss').length;
        return expect(newLength).toBeLessThan(list.length);
      });
    });
  });

}).call(this);
